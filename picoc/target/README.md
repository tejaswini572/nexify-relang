# Pico Go port

Build a precompiled executable from this directory:

```bash
make build
```

Run it with a C program path (the file may use any extension):

```bash
./pico /path/to/program.input
```

Use this exact submission command from `picoc/`:

```bash
./target/pico
```
